This is a Ply transcript: everything the diagram of this document shows — every box, arrow, and rule, and all the text that is otherwise visible only by hovering — written out in full.
It is generated from that document, like a compiler's output, and nothing in it is written by hand. Editing this text changes nothing; edit the ply.yaml document and generate it again.
No result reaches this page. Every line below is a declaration or a promise, never a result — whatever `cargo ply verify` has found, it is reported there and never here.

ply: 1 — the format version every rule below is read under; a version this build does not speak is refused, never guessed at

8 components · 0 functions · 0 promise nothing
("promise nothing" counts functions that end up with nothing checked — whether nobody wrote any checks for them, or the document switched checking off for them on purpose)

components:

  component e2e — maps to Rust module ply_e2e
    note: Drives the built binary the way a user does, and depends on nothing. A suite that links the library instead stops testing what ships. It had grown exactly that dependency once; the fix was to move the test rather than widen the rule.
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

  component attrs — maps to Rust module ply_attrs
    note: Stands alone. It is a proc-macro crate, compiled for the machine doing the building rather than the machine the program will run on, so a dependency from here into the library would compile perfectly well and would be wrong: the macro would begin depending on the very thing it exists to annotate.
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

  component core — maps to Rust module ply_core
    note: Everything Ply knows how to do, with no terminal attached. It must never reach up into the command-line layer: that constraint is what makes the engine adapters testable at all, because the moment a diagnostic's wording lives here, the wording and the engine can only be tested together.
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

  component cli — maps to Rust module ply_cli
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

  component render — maps to Rust module ply_render
    note: Draws a document before any code exists, and writes the same facts as prose. It is the surface a reader judges the whole tool by, which is why its wording is tested by exact string rather than by shape.
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

  component check — maps to Rust module ply_check
    note: The standalone spec validator, which predates the installed `cargo ply check` command and overlaps it. Retiring one of the two is recorded work, not a decision taken here.
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

  component kernel — maps to Rust module ply_kernel
    note: The verdict kernel's own crate: the evidence order, worst-of aggregation and status propagation, kept apart from everything that might tempt it to grow a special case. Its invariants are checked by exhaustive enumeration over every tree up to a bound.
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

  component schedule — maps to Rust module ply_schedule
    note: Check ordering. A second implementation of this ordering also lives inside the verify command, and only this copy has the exhaustive test suite -- recorded as a known gap, because two implementations of a soundness rule is one more than can be trusted.
    hollow — declares nothing inside yet: no functions, no nested components. A sketch waiting for claims.

externals — systems and people outside this codebase: none declared

edges — who may call whom, and what data flows where:
  ("a -> b" means a may call b; that an undeclared cross-component call is forbidden is declared here, and not checked by this build: Ply currently compares crate-level dependencies and does not yet look inside functions, so a call that crosses this line can still go unnoticed (§5.3))
  ("a ~> b : T" means T data flows from a to b — declared so the flow is visible; nothing checks flows in v1)
  cli -> core
  render -> core
  render -> kernel
  check -> core
  kernel -> core
  schedule -> kernel

forbidden calls: none declared

profiles — named bundles of extra rules a component can adopt: none declared

unresolved decisions held by the workspace as a whole: none declared

routes — a public function this document names as the way to build a value of a type Ply has no other way in for: none declared
