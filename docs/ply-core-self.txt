This is a Ply transcript: everything the diagram of this document shows — every box, arrow, and rule, and all the text that is otherwise visible only by hovering — written out in full.
It is generated from that document, like a compiler's output, and nothing in it is written by hand. Editing this text changes nothing; edit the ply.yaml document and generate it again.
No result reaches this page. Every line below is a declaration or a promise, never a result — whatever `cargo ply verify` has found, it is reported there and never here.

ply: 1 — the format version every rule below is read under; a version this build does not speak is refused, never guessed at

11 components · 15 functions · 0 promise nothing
("promise nothing" counts functions that end up with nothing checked — whether nobody wrote any checks for them, or the document switched checking off for them on purpose)

components:

  component core — maps to Rust module ply_core
    note: Everything Ply knows how to do, with no terminal attached.
    state Envelope — the structure this component holds. 4 of 8 of its fields shown
      command is text, written `String`
      root is a structure of your own, written `Node` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
      diagnostics is a list, written `Vec<Diagnostic>` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
      coverage is something that might not be there, written `Option<Coverage>` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
    declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
    that level comes from its weakest part, kernel.StatusSet::len — nothing here counts as checked more strongly than the weakest thing inside it
    component kernel — maps to Rust module ply_core::kernel
      note: Decides what a pile of results adds up to: the order that ranks one kind of evidence above another, the rule that a parent is never reported as stronger than its weakest part, and the flags that travel upward beside a verdict without being confused for one.
      state StatusSet — the structure this component holds
        what must always be true of it, whatever has been done to it:
          state.len() <= 7
        Ply checks each of those by making one of these the only way it can — through the type's own constructor — then calling the type's own operations on it and looking again after every one
        no fields chosen to show — a real state type has many, and naming the ones that matter is the author's job
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, StatusSet::len — nothing here counts as checked more strongly than the weakest thing inside it
      fn StatusSet::len
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: result <= 7 (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
      fn StatusSet::union
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: result.len() >= self.len() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component harness — maps to Rust module ply_core::harness
      note: Works out what a function's inputs are and whether Ply can make one of each. This is the part that decides whether a function can be checked at all, and it is where a refusal gets its reason.
      state ContractFn — the structure this component holds. 4 of 12 of its fields shown
        name is text, written `String`
        params is a list, written `Vec<Param>` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
        requires is something that might not be there, written `Option<(Expr, String)>` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
        ensures is something that might not be there, written `Option<(ExprClosure, String)>` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, last_two_segments — nothing here counts as checked more strongly than the weakest thing inside it
      fn last_two_segments
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: !result.is_empty() || path.is_empty() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
      fn rust_type_from_source
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: result.is_none() || !src.is_empty() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component schedule — maps to Rust module ply_core::schedule
      note: Decides the order claims are verified in, so a callee is checked before a caller that stands on it. The promise below is the one that matters here: a node this dropped would be a claim nobody ever checked, and nothing else would say so.
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, order — nothing here counts as checked more strongly than the weakest thing inside it
      fn order
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          requires: domain.iter().all(|i| *i < node_ids.len()) (what the caller must guarantee going in)
          ensures: |result| result.0.len() + result.1.len() == domain.len() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component surface — maps to Rust module ply_core::surface
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, same_expression — nothing here counts as checked more strongly than the weakest thing inside it
      fn same_expression
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: |result| *result || a != b (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component schema — maps to Rust module ply_core::schema
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, is_identifier — nothing here counts as checked more strongly than the weakest thing inside it
      fn is_identifier
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: |result| !*result || !s.is_empty() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
      fn nearest_key
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: |result| result.is_none() || !known.is_empty() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component engines — maps to Rust module ply_core::engines
      note: The adapters that drive each outside checking tool as a separate program and read back what it said. Ply never re-implements a checker; everything here is translation, in both directions.
      state TimedOutput — the structure this component holds. 3 of 4 of its fields shown
        stdout is a list, written `Vec<u8>`
        stderr is a list, written `Vec<u8>`
        status is a structure of your own, written `ExitStatus` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, strip_ansi — nothing here counts as checked more strongly than the weakest thing inside it
      fn strip_ansi
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: |result| result.len() <= text.len() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component fuzz_gen — maps to Rust module ply_core::fuzz_gen
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, seed_hex — nothing here counts as checked more strongly than the weakest thing inside it
      fn seed_hex
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: |result| result.len() == 64 (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component harness_crate — maps to Rust module ply_core::harness_crate
      note: Writes the generated harness crate. Almost all of it is filesystem work, and that is the honest shape of it -- but the one real piece of reasoning in here was welded to a write until 2026-09-04, and so could not be checked. Split out, it can be.
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, harness_lib_source — nothing here counts as checked more strongly than the weakest thing inside it
      fn harness_lib_source
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: result.1.len() == fn_modules.len() (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
      fn harness_package_name
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: |result| result.ends_with("-ply-harness") (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
      fn harness_rel_path
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: |result| result.starts_with("target/ply/fuzz/") (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component visual — maps to Rust module ply_core::visual
      note: The drawing, the prose form of the same facts, and the portable package an editor reads. Nothing here decides anything; it only shows what was decided.
      state VisualEnvelope — the structure this component holds. 3 of 6 of its fields shown
        svg is text, written `String`
        elements is a lookup table, written `BTreeMap<String, VisualElement>` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
        diagnostics is a list, written `Vec<VisualDiagnostic>` — Ply has no way to make one of these, which is usually why the functions around it come back unchecked
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, stable_element_id — nothing here counts as checked more strongly than the weakest thing inside it
      fn stable_element_id
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: result.starts_with("ply-") (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs
    component record — maps to Rust module ply_core::record
      note: What was checked, and everything a result depended on. A stored result is only reused when every one of those inputs still hashes the same, which is what stops a stale green outliving the code that earned it.
      state RecordEntry — the structure this component holds. 3 of 6 of its fields shown
        verdict is text, written `String`
        statuses is a list, written `Vec<String>`
        inputs is a lookup table, written `BTreeMap<String, String>`
      declares checks up to fuzzed — checked against many random inputs — the strongest verdict this could earn if every declared check ran and passed; a promise, never a result
      that level comes from its weakest part, fingerprint — nothing here counts as checked more strongly than the weakest thing inside it
      fn fingerprint
        fuzz(256) — tries 256 generated inputs that satisfy requires, then checks ensures
        what this function promises — the last thing the document states before the code itself takes over:
          ensures: result.len() == 64 (what the function guarantees coming out)
          the checks above are what will test the function against exactly this promise when `cargo ply verify` runs

externals — systems and people outside this codebase: none declared

edges — who may call whom, and what data flows where: none declared

forbidden calls: none declared

profiles — named bundles of extra rules a component can adopt: none declared

unresolved decisions held by the workspace as a whole: none declared

routes — a public function this document names as the way to build a value of a type Ply has no other way in for: none declared
