interface:
  display_name: "Ply Author"
  short_description: "Write a ply.yaml that says something a run can be wrong about"
  default_prompt: "Use $ply-author to write or extend a ply.yaml. Cover the requested design before code exists, or check and verify each meaningful slice when implementing code."
