[workspace]
# Empty table: this fixture is its own workspace root, per tests/spike's
# convention -- it must never join the product workspace or tools/.

[package]
name = "kanipin-stub_on_contracted"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "kanipin_stub_on_contracted"
path = "src/lib.rs"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(kani)"] }
