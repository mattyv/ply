[workspace]
# Empty table: this crate is its own workspace root, per the tests/spike and
# tests/fixtures convention -- a vetting scenario must never join the product
# workspace at the repository root.

[package]
name = "havoc-given004-ledger"
version = "0.0.0"
edition = "2021"
publish = false

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(kani)"] }

[lib]
name = "ledger"
path = "src/lib.rs"
