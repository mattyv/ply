ply: 1

components:
  receiverstructparam:
    anchor: ply_fixture_receiverstructparam
    state:
      of: Sink
      holds:
        - "state.len() == state.len()"
    fns:
      Sink::ingest:
        checks: [fuzz(64)]
        ensures:
          - "self.len() == old(self.len()) + 1"
