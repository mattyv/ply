ply: 1

# docs/review-structs-enums.md finding 2, "is the disclosure enough? -- No":
# `Point::labeled` is a real constructor Ply's scan finds but cannot use
# (its `&str` argument is unbuildable), so Ply correctly falls through to
# direct field construction -- but the W0522 disclosure must say a
# constructor was found and skipped, and why, not read as though field
# construction were the only route.
components:
  skippedctor:
    anchor: ply_fixture_skippedctor
    fns:
      always_nonneg:
        checks: [fuzz(64)]
