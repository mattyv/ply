ply: 1

# Composition (TODO.md, "make the sampling engine's decision recursive"),
# 2026-09-02: `width`'s own parameter is `Option<String>` -- until this
# task, a type Ply's ordinary codegen never built at all (`String` was
# deliberately never nested), refused outright with no `examples:` entry
# able to unblock it. It is now fuzz-supported directly, exactly like any
# other buildable shape, no seed required.
components:
  paramseeded:
    anchor: ply_fixture_paramseeded
    fns:
      width:
        checks: [fuzz(64)]
