ply: 1

components:
  reusebuildinputs:
    anchor: ply_fixture_reusebuildinputs
    fns:
      answer:
        requires:
          - "x <= 100"
        ensures:
          - "|result| *result <= 7"
        checks: [fuzz(64)]
