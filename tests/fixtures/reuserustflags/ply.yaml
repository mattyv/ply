ply: 1

components:
  reuserustflags:
    anchor: ply_fixture_reuserustflags
    fns:
      answer:
        requires:
          - "x <= 100"
        ensures:
          - "|result| *result <= 7"
        checks: [fuzz(64)]
