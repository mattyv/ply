ply: 1

# docs/review-silent-narrowing.md finding 1, "the type's only mutator lives
# in a second file". `till::Till::take`, the only operation that changes
# `Till`'s state, is declared in a second `impl Till` block in `more.rs` --
# a different file than `till.rs`, where `Till` itself and the checked
# method `total` are declared. `total`'s promise (always 0) is false after
# one ordinary `take` call, and the receiver scan must open `more.rs` to
# find `take` at all.
components:
  crossfilemutator:
    anchor: ply_fixture_crossfilemutator
    fns:
      till::Till::total:
        checks: [fuzz(256)]
