ply: 1

# docs/review-structs-enums.md's "Also fix" list: "a crash in a function
# whose only parameter is a struct loses its witness". `width` panics
# whenever `end < start` -- proptest's shrunk minimal input describes
# `Window`'s two fields, never one value per declared parameter, and the
# plain positional recovery used to discard the witness on that count
# mismatch, reporting a tool error instead of the real crash.
components:
  structparamcrash:
    anchor: ply_fixture_structparamcrash
    fns:
      width:
        checks: [fuzz(256)]
