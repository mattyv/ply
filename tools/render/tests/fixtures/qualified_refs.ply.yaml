ply: 1
components:
  alpha:
    anchor: app::alpha
    components:
      shared:
        anchor: app::alpha::shared
  beta:
    anchor: app::beta
    components:
      shared:
        anchor: app::beta::shared
  sink:
    anchor: app::sink

edges:
  - "alpha.shared -> sink"
  - "beta.shared -> sink"
  - "sink -> alpha"
